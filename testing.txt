Note: Please note that some logs given for clarity purpose in this file are not most upto date with the current version of the program, for the latest logs, please run the program with commands given here.

Test cases

-------------- Client -----------------------

-- <Test Case>
    Description:-

    Command:-
    python3 -m da --logfile --logfilename=logs/-log.txt --logfilelevel=info src/bcr.da -i config/.txt

-------------- Olympus -----------------------

-- <Test Case>
    Description:-

    Command:-
    python3 -m da --logfile --logfilename=logs/-log.txt --logfilelevel=info src/bcr.da -i config/.txt

-------------- Replicas -----------------------

-- <Test Case>
    Description:-

    Command:-
    python3 -m da --logfile --logfilename=logs/-log.txt --logfilelevel=info src/bcr.da -i config/.txt

---------- Stress test ------------------------
-- <Test Case>
    Description:-

    Command:-
    python3 -m da --logfile --logfilename=logs/-log.txt --logfilelevel=info src/bcr.da -i config/.txt

---------- RAFT Comparison --------------------
-- <Test Case>
    Description:-

    Command:-
    python3 -m da --logfile --logfilename=logs/-log.txt --logfilelevel=info src/bcr.da -i config/.txt